# Anthropic API key used by the LangGraph agent (Claude Haiku 4.5).
ANTHROPIC_API_KEY=

# Token required by the fictitious JIRA MCP server. Leave unset to see the
# agent gracefully report that it needs a token before it can create tickets.
MOCK_JIRA_API_TOKEN=
